{
  "name": "seomtorch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve .",
    "check": "node --check app.js && node --check scripts/build-question-bank.mjs",
    "questions:build": "node scripts/build-question-bank.mjs"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
